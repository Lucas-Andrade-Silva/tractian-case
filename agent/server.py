"""Ponto de entrada do agente (placeholder — ainda sem arquitetura interna definida).

Sobe em AGENT_PORT (ver ../Makefile, alvo up-agent). Por enquanto só confirma que a
estrutura de pastas está no lugar; a lógica real (tools, grafo/loop, trace) ainda
não foi implementada — depende da decisão de arquitetura (ADR 0001 vs. ReAct único).
"""

if __name__ == "__main__":
    print("agent/server.py: esqueleto criado, lógica ainda não implementada.")
